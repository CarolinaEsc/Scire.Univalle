@{
    var db = Database.Open("connectionString");

    var insertQuery = @"UPDATE CommentNote
                        set status = 0
                        where idCommentNote = @0";
    db.Execute(insertQuery, Request.Cookies["commentId"].Value);
    Request.Cookies["commentId"].Expires = DateTime.Now.AddSeconds(-1);
    Response.Redirect("~/ni1.cshtml");

}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <div>
    
    </div>
</body>
</html>
